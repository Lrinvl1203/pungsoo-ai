# PUNGSOO AI — 공통 에이전트 작업 규칙

이 파일은 Codex, Claude Code와 그 밖의 코딩 에이전트가 공유하는 최상위
작업 규칙이다. 대화 기억보다 저장소의 코드, Git 상태와 아래 정본 문서를
우선한다.

## 1. 작업 시작 순서

모든 에이전트는 수정 전에 반드시 다음 순서로 확인한다.

1. 현재 작업 경로가
   `P:\0_지키기\02_PROJECT\99_Working\41_pungsoo-ai`인지 확인한다.
2. `git status --short`, 현재 브랜치와 `git rev-parse HEAD`를 확인한다.
3. 이 파일과 `CLAUDE.md`를 읽는다.
4. `docs/PROJECT_INDEX.md`를 읽는다.
5. `docs/PROJECT_STATE.md`와 `docs/HANDOFF.md`를 읽는다.
6. 작업 주제에 해당하는 전문 문서를 인덱스에서 찾아 읽는다.

다른 프로젝트의 대화나 추측을 이 프로젝트의 사실로 가져오지 않는다.

## 2. 정본 우선순위

충돌할 때 다음 순서를 따른다.

1. 현재 실행 코드와 검증된 외부 시스템 상태
2. `docs/PROJECT_STATE.md`
3. `docs/DECISION_LOG.md`
4. `docs/PROMPT_INDEX.md`와 그 문서가 지정한 런타임 소스
5. 주제별 전문 문서
6. README, 과거 연구 문서와 대화 기록

오래된 문서를 조용히 삭제하거나 사실처럼 사용하지 않는다. 현재와 다르면
상단에 `SUPERSEDED`를 표시하고 새 정본으로 링크한다.

## 3. 기록 갱신 계약

아래 변경을 하면 같은 작업에서 대응 문서를 함께 갱신한다.

| 변경 | 반드시 갱신할 문서 |
|---|---|
| 기능, 사용자 흐름, API, 배포 | `PROJECT_STATE.md`, `HANDOFF.md` |
| 중요한 제품·기술 선택 | `DECISION_LOG.md` |
| 분석·이미지·오브제 프롬프트 또는 모델 | `PROMPT_INDEX.md`, 필요 시 전문 프롬프트 문서 |
| 액자·오브제 생산 파일, 주문, 수령, QC | `physical-product-mvp-status.md`, `PROJECT_STATE.md` |
| 활성 TODO의 우선순위나 상태 | `PROJECT_STATE.md`, `HANDOFF.md` |
| 외부 업체·가격·정책 조사 | 조사일, URL, 확인 사실과 추정 구분 |

완료한 일을 TODO에 남겨두지 않는다. 완료·보류·폐기된 판단을 구분한다.

## 4. 프롬프트 관리

- 실제 서비스 프롬프트의 최종 정본은 `docs/PROMPT_INDEX.md`가 지정한 코드다.
- 연구용 메타 프롬프트와 런타임 프롬프트를 혼동하지 않는다.
- 프롬프트를 변경할 때 목적, 입력 변수, 모델, 출력 형식, 금지 항목,
  평가 방법과 변경 이유를 기록한다.
- 특정 작가 이름을 직접 복제하는 요청은 작품군·시대·재료·구도 특성으로
  변환한다.
- 비방화와 오브제는 동일한 수호동물·오행·에너지 모드를 공유한다.

## 5. Git과 동시 작업

- 사용자의 기존 변경과 untracked 파일을 보존한다.
- 현재 `artifacts/print-orders/`는 사용자 소유의 untracked 자료로 취급하며
  명시적 요청 없이 이동·삭제·커밋하지 않는다.
- 두 에이전트가 같은 worktree를 동시에 수정하지 않는다.
- 병렬 작업이 필요하면 사용자 승인 후 별도 branch/worktree를 사용한다.
- `git reset --hard`, 무단 checkout, stash, 재배포와 외부 결제를 하지 않는다.
- 커밋·푸시·배포 권한은 현재 요청과 인계문의 범위를 따른다.

## 6. 보안과 개인정보

- `.env.local`, API 키, 토큰, 결제 비밀키, 개인 주소·전화번호를 문서나
  Git에 기록하지 않는다.
- 사진 EXIF 위치는 사용 목적과 동의를 확인하고 최소한으로 저장한다.
- 실제 고객 자료는 샘플 문서에 직접 복사하지 말고 익명화한다.

## 7. 검증과 종료

코드 변경 후 작업 범위에 맞게 아래를 실행한다.

```powershell
npm run build
npm run docs:check
git status --short
```

종료 전에:

1. 변경 파일과 테스트 결과를 확인한다.
2. 현재 상태·결정·프롬프트·TODO 문서가 실제와 일치하는지 확인한다.
3. `docs/HANDOFF.md`에 다음 에이전트가 바로 시작할 위치를 남긴다.
4. 미완료·위험·사용자 확인 필요 사항을 숨기지 않는다.
5. 커밋·푸시·배포 여부를 최종 보고한다.
